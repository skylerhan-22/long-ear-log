# Long Ear Log（长耳日志）产品文档

> 版本：1.0  
> 状态：功能与视觉冻结  
> 定稿日期：2026-08-15  
> 产品形态：移动端优先的健身与饮食记录 PWA

## 1. 产品概述

Long Ear Log 是一款把训练计时、训练完成记录和饮食日志放在一起的轻量记录工具。它不替用户制定训练或饮食计划，而是降低记录负担，让用户看清自己本周做了什么、今天吃了什么。

产品使用黑白漫画界面和兔子角色。兔子不是评分者，而是陪伴用户记录的视觉角色。

## 2. 1.0 目标

- 用户可以自行配置一周七天的训练内容。
- 用户可以进入任意训练日，主动开始计时并逐组打勾。
- 用户可以记录一餐的照片、热量和可选三大营养素。
- 用户可以按日、周、月回顾饮食记录。
- 用户可以安装为 PWA，并在基础离线场景下打开应用。
- 页面保持纯黑白、粗线条、漫画感和统一兔子形象。

## 3. 1.0 不做的内容

- 不自动生成训练计划。
- 不自动生成饮食计划或规定用户必须吃什么。
- 不提供减脂、增肌或疾病相关的医疗建议。
- 不把性别直接映射为固定的运动或饮食方案。
- 不提供真实账号、云同步、好友、社区和排行榜。
- 图片热量识别在 1.0 Demo 中只展示流程，不代表真实模型结果。

## 4. 用户与产品原则

### 4.1 目标用户

- 希望自由安排训练内容的人。
- 需要训练计时和逐组记录的人。
- 希望用照片和少量数字记录饮食的人。
- 不喜欢复杂计划或严格身体评分的人。

### 4.2 性别与女性友好原则

1.0 的核心是记录，不根据性别自动给出训练或热量处方。注册信息保留姓名、身高、体重和健身目标，用于个人展示与后续可选功能。

- 不默认女性只做低重量、臀腿或减脂训练。
- 不默认男性需要更高热量或更高强度训练。
- 热量和三大营养素都由用户确认，AI 结果必须可修改。
- 不使用羞辱、惩罚、补偿性运动或身体排名文案。
- 后续如增加生理周期、孕产或特殊营养功能，必须作为可选信息并单独验证安全性。

## 5. 信息架构

底部导航固定为四个一级入口：

1. 今日
2. 训练
3. 饮食
4. 我的

训练计时、训练编辑和饮食记录使用独立全屏页面，避免在底部弹窗中堆叠复杂操作。

## 6. 功能规格

### 6.1 今日

今日页聚合当天最需要处理的内容：

- 问候语与本周训练完成进度。
- 今日训练名称、动作数、组数和预计时间。
- 开始训练、继续训练或查看训练总结。
- 今日总热量。
- 早餐、午餐、晚餐、零食四类入口及各自热量。

兔子状态：

- 默认：站立挥手。
- 今日训练完成：站立并双手举高庆祝。

### 6.2 训练

#### 本周训练

- 展示周一到周日七个条目。
- 过去、今天和未来的日期都可以点击查看。
- 每行显示计划名称、动作数、总组数和状态。
- 休息日显示“恢复与休息”。

#### 动态周目标

- 周目标等于本周包含至少一个动作的训练日数量。
- 休息日不计入目标。
- 用户增加或减少训练日后，今日页、本周训练页和我的页同步更新。
- 完成进度公式：`已完成训练日数 / 本周训练日总数`。
- 只有所有计划训练日都完成时，才显示完成状态兔子。
- 周进度兔子不使用固定数量的勾选框，避免暗示必须每周训练四次。

#### 编辑训练计划

用户可以编辑七天中的任意一天：

- 训练名称。
- 训练类型及对应配图。
- 训练日或休息日。
- 动作名称。
- 组数、次数和重量。
- 添加或删除动作。

自定义名称会先通过关键词推荐训练类型；用户可手动覆盖类型与配图。

#### 训练详情与计时

- 点击某一天进入该日详情。
- 返回时回到本周训练列表，不跳转到错误页面。
- 开始训练后，训练时钟首先占据约半屏。
- 计时必须由用户主动点击“开始计时”。
- 向上滚动动作列表后，时钟收拢到顶部。
- 每组由用户独立打勾。
- 所有组完成前，“完成训练”按钮不可用。
- 提供 60 秒组间休息计时，可取消。

### 6.3 饮食

#### 记录一餐

饮食记录是独立页面，流程如下：

1. 拍照或选择图片。
2. 选择 AI 辅助估算或手动输入。
3. 输入或确认热量。
4. 可选输入蛋白质、碳水、脂肪。
5. 选择早餐、午餐、晚餐或零食。
6. 保存到饮食日志。

规则：

- 热量单位固定为 kcal。
- 三大营养素单位固定为 g。
- 热量必填，三大营养素可选。
- AI 估算必须允许用户修改。
- 1.0 的 AI 按钮是流程演示，并明确标注 Demo。

#### 日视图

- 显示当天每条饮食记录。
- 每条显示餐别、热量和三大营养素。

#### 周视图

使用七行列表，一天一行：

- 左侧：星期与兔子打卡徽章；无记录显示加号。
- 中间：早餐、午餐、晚餐、零食四个状态。
- 黑底圆点表示已记录，空心圆点表示未记录。
- 右侧：当天总热量；无记录显示 `— kcal`。

#### 月视图

- 使用月历回顾有记录的日期。
- 有记录日期显示兔子打卡徽章。
- 支持切换上个月和下个月。
- 月底摘要用于显示记录天数，不进行好坏评分。

### 6.4 我的

- 显示姓名、目标、身高、体重和动态周训练目标。
- 提供健身目标说明、单位设置、计时提醒和离线模式入口。
- 兔子动作是站立并双手抱着训练清单。
- 1.0 中设置项使用说明型弹窗或本地切换，不连接真实账号服务。

## 7. 视觉规范

### 7.1 色彩

- 页面只使用纯黑与纯白。
- 不使用黄色、米色、灰色填充或彩色状态。
- 选中状态使用黑底白字，未选中状态使用白底黑线。

### 7.2 线条与组件

- 卡片使用 1.5–3px 黑色边框。
- 关键卡片可使用纯黑偏移阴影。
- 按钮、日历格和列表使用明确边界，避免依赖颜色区分。
- 交互控件保持适合触屏的点击区域。

### 7.3 兔子角色

- 白色身体、粗黑轮廓、大圆眼、长耳朵和较大的脚。
- 身体比例在今日、训练、饮食和我的页面保持一致。
- 图片使用透明背景，不保留白色矩形底。
- 耳朵、手和脚必须完整，不被容器裁切。
- 动作用于解释状态，不用于评价用户表现。

## 8. 核心数据结构

### 8.1 训练日

- 星期与简称
- 计划名称
- 训练类型
- 状态：已完成、今日、待完成、休息
- 动作列表

### 8.2 动作

- 动作 ID
- 动作名称
- 组数
- 次数
- 重量或自重说明

### 8.3 饮食记录

- 记录 ID
- 餐别
- 热量
- 蛋白质（可选）
- 碳水（可选）
- 脂肪（可选）
- 图片（正式版字段）

## 9. PWA 与技术范围

- 移动端优先响应式 Web App。
- 支持 Web App Manifest 和 Service Worker。
- 核心静态页面与兔子素材加入离线缓存。
- 1.0 Demo 数据保存在页面状态中，刷新后恢复演示数据。
- 正式产品需要增加本地持久化或云端数据库、身份验证、隐私授权和图片存储。

## 10. 1.0 关键改动记录

### 训练

- 修复训练详情返回路径。
- 七天训练均可查看和编辑。
- 编辑器支持动作、组数、次数、重量和训练类型。
- 训练时间改为用户主动开启。
- 完成按钮改为全部组完成后启用。
- 周训练目标由固定 4 次改为根据用户计划动态计算。
- 为不同运动类型配置独立兔子配图。

### 饮食

- 饮食记录从上拉框改为独立页面。
- 增加拍照、AI 演示估算和手动输入。
- 增加日、周、月三种回顾方式。
- 周视图从两排日历改为七天列表。
- 周列表增加四餐记录状态与每日总热量。
- 周历和月历统一使用用户选定的兔子打卡徽章。

### 视觉

- 页面从暖黄色方案改为纯黑白方案。
- 清理图片白底、裁切和比例问题。
- 训练配图改为独立透明素材。
- 周进度兔子改为统一站姿并移除固定次数暗示。
- 今日和我的兔子保留原动作，统一为训练配图的粗线条与身体比例。

## 11. 1.0 验收标准

- 四个底部导航入口均可使用。
- 所有可见按钮无无响应问题。
- 七天计划均可打开，编辑内容保存后立即更新。
- 训练计时不会自动启动。
- 未完成全部组时无法完成训练。
- 自定义训练日数量后，所有周目标数字同步。
- 饮食可通过手动模式保存记录。
- AI 模式明确说明为 Demo，不伪装为真实识别。
- 周饮食列表正确显示四餐状态和总热量。
- 月历正确显示兔子打卡徽章。
- 页面不出现灰色或彩色填充。
- 兔子图片没有白底、低清晰度或明显裁切。
- 应用构建通过，自动测试通过，浏览器控制台无错误。

## 12. 已知限制与 1.1 候选项

- 数据刷新后不会保留。
- 注册与登录尚未实现。
- AI 热量识别尚未连接真实视觉模型。
- 食物数据库、份量校正和条码扫描尚未实现。
- 周视图中的历史日数据目前为演示数据。
- 月历尚未连接真实日期和历史记录。
- 未实现训练模板导入、动作库搜索和历史趋势图。
- 未实现图片压缩、云端存储、导出和隐私删除流程。

1.0 在以上边界内完成后冻结。新增能力进入 1.1 评估，不继续扩展 1.0 范围。
