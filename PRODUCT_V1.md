# Long Ear Log（长耳日志）产品文档

> 版本：1.0  
> 状态：功能与视觉冻结  
> 定稿日期：2026-08-15  
> 产品形态：移动端优先的健身与饮食记录 PWA

## 1. 产品概述

Long Ear Log 是一款把训练计时、训练完成记录和饮食日志放在一起的轻量记录工具。它不替用户制定训练或饮食计划，而是降低记录负担，让用户看清自己本周做了什么、今天吃了什么。

产品使用黑白漫画界面和兔子角色。兔子不是评分者，而是陪伴用户记录的视觉角色。

## 2. 1.0 目标

- 用户可以自行配置一周七天的训练内容。
- 用户可以进入任意训练日，主动开始计时并逐组打勾。
- 用户可以记录一餐的照片、热量和可选三大营养素。
- 同一餐别可以记录任意多条内容，尤其支持把每次零食和加餐分开保存。
- 用户可以按日、周、月回顾饮食记录。
- 训练计划、训练进度、训练历史和饮食记录刷新后仍可恢复。
- 用户可以安装为 PWA，并在基础离线场景下打开应用。
- 页面保持纯黑白、粗线条、漫画感和统一兔子形象。

## 3. 1.0 不做的内容

- 不自动生成训练计划。
- 不自动生成饮食计划或规定用户必须吃什么。
- 不提供减脂、增肌或疾病相关的医疗建议。
- 不把性别直接映射为固定的运动或饮食方案。
- 不提供真实云端账号、跨设备同步、好友、社区和排行榜；1.0 仅提供明确标注的本机账号演示。
- 图片热量识别在 1.0 Demo 中只展示流程，不代表真实模型结果。

## 4. 用户与产品原则

### 4.1 目标用户

- 希望自由安排训练内容的人。
- 需要训练计时和逐组记录的人。
- 希望用照片和少量数字记录饮食的人。
- 不喜欢复杂计划或严格身体评分的人。

### 4.2 性别与女性友好原则

1.0 的核心是记录，不根据性别自动给出训练或热量处方。注册信息保留姓名、身高、体重和健身目标，用于个人展示与后续可选功能。

- 不默认女性只做低重量、臀腿或减脂训练。
- 不默认男性需要更高热量或更高强度训练。
- 热量和三大营养素都由用户确认，AI 结果必须可修改。
- 不使用羞辱、惩罚、补偿性运动或身体排名文案。
- 后续如增加生理周期、孕产或特殊营养功能，必须作为可选信息并单独验证安全性。

## 5. 信息架构

底部导航固定为四个一级入口：

1. 今日
2. 训练
3. 饮食
4. 我的

训练计时、训练编辑和饮食记录使用独立全屏页面，避免在底部弹窗中堆叠复杂操作。

## 6. 功能规格

### 6.1 今日

今日页聚合当天最需要处理的内容：

- 问候语与本周训练完成进度。
- 今日训练名称、动作数、组数和预计时间。
- 开始训练、继续训练或查看训练总结。
- 今日总热量。
- 早餐、午餐、晚餐、零食四类入口及各自热量。

兔子状态：

- 默认：站立挥手。
- 今日训练完成：站立并双手举高庆祝。

### 6.2 训练

#### 本周训练

- 展示周一到周日七个条目。
- 过去、今天和未来的日期都可以点击查看。
- 每行显示计划名称、动作数、总组数和状态。
- 休息日显示“恢复与休息”。

#### 动态周目标

- 周目标等于本周包含至少一个动作的训练日数量。
- 休息日不计入目标。
- 用户增加或减少训练日后，今日页、本周训练页和我的页同步更新。
- 完成进度公式：`已完成训练日数 / 本周训练日总数`。
- 只有所有计划训练日都完成时，才显示完成状态兔子。
- 周进度兔子不使用固定数量的勾选框，避免暗示必须每周训练四次。

#### 编辑训练计划

用户可以编辑七天中的任意一天：

- 训练名称。
- 训练类型及对应配图。
- 训练日或休息日。
- 动作名称。
- 组数、次数和重量。
- 添加或删除动作。

自定义名称会先通过关键词推荐训练类型；用户可手动覆盖类型与配图。

#### 训练详情与计时

- 点击某一天进入该日详情。
- 返回时回到本周训练列表，不跳转到错误页面。
- 开始训练后，训练时钟首先占据约半屏。
- 计时必须由用户主动点击“开始计时”。
- 向上滚动动作列表后，时钟收拢到顶部。
- 每组由用户独立打勾。
- 所有组完成前，“完成训练”按钮不可用。
- 提供 60 秒组间休息计时，可取消。
- 退出或刷新训练页面后，已用时间与已勾选组数可以恢复；计时依据时间戳计算，应用切到后台也不会丢失经过时间。
- 完成训练后显示本次用时、完成组数、动作清单和训练总结。
- 训练历史可查看每次训练详情、补充体感备注并删除记录。
- 训练页包含“本周计划 / 训练历史 / 训练数据”三个并列视图。
- 训练数据页汇总动作总数、累计训练与完成组数；选择任一动作可查看重量变化。完成记录达到条件后使用真实重量数据，记录不足时明确标注演示趋势。

### 6.3 饮食

#### 记录一餐

饮食记录是独立页面，流程如下：

1. 拍照或选择图片。
2. 选择 AI 辅助估算或手动输入。
3. 输入或确认热量。
4. 可选输入蛋白质、碳水、脂肪。
5. 选择早餐、午餐、晚餐或零食。
6. 保存到饮食日志。

规则：

- 热量单位固定为 kcal。
- 三大营养素单位固定为 g。
- 热量必填，三大营养素可选。
- AI 估算必须允许用户修改。
- 1.0 的 AI 按钮是流程演示，并明确标注 Demo；识别后先展示食物、份量、单项热量和置信度，用户确认后才填写总热量与三大营养素。
- 早餐、午餐、晚餐和零食都不是单次覆盖字段；每次保存都会生成一条独立记录。
- 同一餐别有多条记录时，今日页显示记录条数与合计热量，点击后进入该餐别的记录列表。
- 每条饮食记录都可以单独查看、编辑、收藏为常用餐食或删除。
- 新增记录页提供最近记录和常用餐食，点击即可快速复用到当前餐别。

#### 日视图

- 顶部营养摘要只在日视图出现，周、月视图不重复展示。
- 不显示 P、C、F 字母缩写，直接显示蛋白质、碳水、脂肪完整中文名称和克数。
- 显示当天每条饮食记录。
- 每条显示餐别、热量和三大营养素。

#### 周视图

使用七行列表，一天一行：

- 左侧：星期与兔子打卡徽章；无记录显示加号。
- 中间：早餐、午餐、晚餐、零食四个漫画小图状态。
- 已记录图标完整显示并带短下划线，未记录图标降低透明度。
- 右侧：当天总热量；无记录显示 `— kcal`。

#### 月视图

- 使用月历回顾有记录的日期。
- 有记录日期显示兔子打卡徽章。
- 支持切换上个月和下个月；日期格、星期顺序和跨月日期根据真实月份计算。
- 徽章只由当前月份的真实饮食记录生成，切换月份后同步更新。
- 点击任意日期进入该日视图，可查看记录或补记一餐。
- 月底摘要用于显示记录天数，不进行好坏评分。

### 6.4 我的

- 显示姓名、目标、身高、体重和动态周训练目标。
- 支持编辑姓名、目标、身高和体重；切换公制与英制时同步换算数值。
- 提供本周总结：训练次数、总训练时间、饮食记录天数、平均热量、7 天热量记录和近 4 周训练次数。
- 提供本机账号演示、设备保存状态、JSON 完整备份、饮食 CSV 导出、隐私说明和删除本机数据流程。
- 提供健身目标说明、单位设置、计时提醒和离线模式入口。
- 兔子动作是站立并双手抱着训练清单。
- 1.0 中设置项使用说明型弹窗或本地切换，不连接真实账号与云同步服务，并在界面内明确说明这一限制。

## 7. 视觉规范

### 7.1 色彩

- 页面组件只使用纯黑与纯白。
- 状态不依赖彩色区分；唯一例外是兔子 IP 保留母版的暖米白身体、浅粉眼白和极轻的灰米色阴影。
- 选中状态使用黑底白字，未选中状态使用白底黑线。

### 7.2 线条与组件

- 卡片使用 1.5–3px 黑色边框。
- 关键卡片可使用纯黑偏移阴影。
- 按钮、日历格和列表使用明确边界，避免依赖颜色区分。
- 交互控件保持适合触屏的点击区域。

### 7.3 兔子角色

- 以 `public/mascot-reference.png` 为唯一 IP 母版：暖米白长椭圆身体、手绘粗黑轮廓、外凸浅粉椭圆眼、细长直立耳朵、细长手臂、短腿和平脚。
- 身体比例在今日、训练、饮食和我的页面保持一致。
- 图片使用透明背景，不保留白色矩形底。
- 耳朵、手和脚必须完整，不被容器裁切。
- 动作用于解释状态，不用于评价用户表现。
- 所有场景使用独立高清 PNG；饮食四餐使用近景构图，其余状态和训练类型使用完整全身构图。

## 8. 核心数据结构

### 8.1 训练日

- 星期与简称
- 计划名称
- 训练类型
- 状态：已完成、今日、待完成、休息
- 动作列表

### 8.2 动作

- 动作 ID
- 动作名称
- 组数
- 次数
- 重量或自重说明

### 8.3 饮食记录

- 记录 ID
- 餐别
- 记录名称
- 热量
- 蛋白质（可选）
- 碳水（可选）
- 脂肪（可选）
- 创建时间
- 图片（本地保存）
- 是否为常用餐食

### 8.4 训练历史

- 记录 ID 与完成时间
- 星期、训练名称和训练类型
- 本次训练用时
- 完成组数与总组数
- 完成时的动作清单快照
- 用户备注

## 9. PWA 与技术范围

- 移动端优先响应式 Web App。
- 支持 Web App Manifest 和 Service Worker。
- 核心静态页面与兔子素材加入离线缓存。
- 1.0 Demo 使用设备本地存储保存训练计划、训练中的状态、训练历史、饮食记录和常用餐食。
- 训练计时和组间休息使用时间戳恢复，降低切换后台或刷新页面造成的记录丢失。
- 当前数据只保存在这台设备的这个浏览器中；用户可导出 JSON/CSV 备份，也可在应用内删除个人记录。
- 本机账号只演示账号入口与状态，不提供身份验证或跨设备同步。
- 正式产品仍需增加真实认证、云同步、数据库、隐私授权、图片对象存储和跨设备冲突处理。

## 10. 1.0 关键改动记录

### 训练

- 修复训练详情返回路径。
- 七天训练均可查看和编辑。
- 编辑器支持动作、组数、次数、重量和训练类型。
- 训练时间改为用户主动开启。
- 完成按钮改为全部组完成后启用。
- 周训练目标由固定 4 次改为根据用户计划动态计算。
- 为不同运动类型配置独立兔子配图。
- 新增独立“训练数据”页，按动作汇总训练次数、当前重量与重量变化折线。

### 饮食

- 饮食记录从上拉框改为独立页面。
- 增加拍照、AI 演示估算和手动输入。
- 增加日、周、月三种回顾方式。
- 周视图从两排日历改为七天列表。
- 周列表增加四餐记录状态与每日总热量。
- 周历和月历统一使用用户选定的兔子打卡徽章。
- 日视图营养摘要改为无内框的中文营养指标，并从周、月视图移除。
- 周列表的四餐文字缩写改为早餐、午餐、晚餐、零食漫画小图。
- 月历改为真实日期计算、真实记录驱动徽章和可点击回填日期。

### 视觉

- 页面从暖黄色方案改为纯黑白方案。
- 清理图片白底、裁切和比例问题。
- 为今日、我的、周进度、训练详情和训练记录卡统一增加图片安全区，禁止耳朵或脚超出边框。
- 训练配图改为独立透明素材。
- 周进度兔子改为统一站姿并移除固定次数暗示。
- 今日和我的兔子保留原动作，统一为训练配图的粗线条与身体比例。
- 1.0 最终素材重新回到 `mascot-reference.png` 的原始 IP 设计，并成套重做今日、我的、周进度、七类训练、六类饮食场景、打卡徽章、App 图标和分享封面。
- 旧的拼图式饮食素材不再参与页面渲染，四餐改为独立透明 PNG，避免缩放裁切导致的低清晰度。

### 功能补完（1–9）

1. 增加本地持久化，训练和饮食数据刷新后可恢复。
2. 增加训练完成总结，展示时间、组数和动作清单。
3. 增加训练历史与饮食单条详情。
4. 增加饮食编辑、收藏、删除，以及训练备注和历史删除。
5. 增加训练中断恢复、后台时间戳计时和休息计时恢复。
6. 增加最近记录、常用餐食和一键复用。
7. 增加 AI 识别结果确认层：先核对食物和份量，再填写可编辑的热量与三大营养素；当前仍是明确标注的 Demo 数据。
8. 增加本周总结与基础趋势，包括训练、训练时间、饮食记录天数、平均热量、7 天饮食和近 4 周训练概览。
9. 增加个人资料编辑、本机账号演示、JSON/CSV 导出、隐私说明与数据删除流程；跨设备云同步明确显示为“未连接”。

补充：饮食数据模型已调整为“一餐别多条记录”，早餐、午餐、晚餐、零食与加餐都可不限次数连续添加。

## 11. 1.0 验收标准

- 四个底部导航入口均可使用。
- 所有可见按钮无无响应问题。
- 七天计划均可打开，编辑内容保存后立即更新。
- 训练计时不会自动启动。
- 未完成全部组时无法完成训练。
- 自定义训练日数量后，所有周目标数字同步。
- 饮食可通过手动模式保存记录。
- AI 模式明确说明为 Demo，不伪装为真实识别。
- AI 演示必须先确认食物与份量，确认前不自动写入营养数据。
- 周饮食列表正确显示四餐状态和总热量。
- 训练数据页可切换动作并更新对应重量趋势。
- 日营养摘要能同时辨认 P/C/F 与完整中文名称，并且只出现在日视图。
- 月历按真实月份显示 42 个日期格，切换月份后徽章与记录天数同步变化。
- 点击月历日期能进入对应日视图并补记。
- 本周总结能根据本机数据更新基础指标。
- JSON 与 CSV 可以导出；删除流程必须二次确认并清除个人记录。
- 账号页面明确区分“设备内保存”与“云同步未连接”。
- 页面不出现灰色或彩色填充。
- 兔子图片没有白底、低清晰度或明显裁切。
- 应用构建通过，自动测试通过，浏览器控制台无错误。

## 12. 已知限制与 1.1 候选项

- 数据可在同一浏览器刷新后恢复，但尚未支持跨设备同步与备份导入。
- 账号入口仅为本机交互演示，尚未连接真实身份验证。
- AI 热量识别尚未连接真实视觉模型。
- 食物数据库、份量校正和条码扫描尚未实现。
- 周视图中的历史日数据目前为演示数据。
- 月历已连接本机饮食记录，但还没有跨设备历史。
- 已提供基础趋势图，尚未实现日期筛选、训练量曲线和营养目标区间。
- 尚未实现训练模板导入、动作库搜索和备份导入。
- 图片仍以浏览器本地数据形式保存，尚未实现压缩与云端对象存储。

1.0 在以上边界内完成后冻结。新增能力进入 1.1 评估，不继续扩展 1.0 范围。
